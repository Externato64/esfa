import React from "react";
import { Container, ModalArea } from "./styles";

export const LoadingModal = (): JSX.Element => {
    return (
        <Container>
            <ModalArea />
        </Container>
    );
};